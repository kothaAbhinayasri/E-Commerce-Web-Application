# Sample .env file for E-Commerce Web Application

# Google OAuth Credentials
GOOGLE_CLIENT_ID=your-google-client-id.apps.googleusercontent.com
GOOGLE_CLIENT_SECRET=your-google-client-secret
GOOGLE_CALLBACK_URL=http://localhost:5000/api/auth/google/callback

# Frontend URL (used for redirect after login)
FRONTEND_URL=http://localhost:5173

# Gmail SMTP Credentials for sending emails
EMAIL_USER=your-email@gmail.com
EMAIL_PASS=your-email-app-password

# MongoDB connection string (example)
MONGODB_URI=mongodb://localhost:27017/ecommerce-db

# JWT secret key
JWT_SECRET=your_jwt_secret_key

# Server port
PORT=5000

# Instructions:
# 1. Replace the placeholder values with your actual credentials.
# 2. For Google OAuth credentials, create a project in Google Cloud Console,
#    enable OAuth 2.0, and set the authorized redirect URI to the value of GOOGLE_CALLBACK_URL.
# 3. For EMAIL_PASS, generate an App Password in your Google Account security settings if using 2FA.
# 4. Restart your backend server after updating this file to load the new environment variables.
